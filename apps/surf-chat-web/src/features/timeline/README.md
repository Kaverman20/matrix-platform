# Timeline

Timeline rendering, message grouping, day dividers, message rows, bubbles, and media slots.

Current scope:

- basic `m.room.message` rendering
- simple consecutive-message grouping
- automatic scroll to latest message

Reply, reactions, edits UI, media, pins, and threads are migrated later.
