# Mapping API

Future home of the FastAPI service for Keycloak group to Matrix Space/Room rules.

The legacy implementation currently lives in the old `Matrix x Element/mapping-api`
workspace and should be migrated intentionally, not copied wholesale.

