# Shared defaults
VITE_DEFAULT_HOMESERVER=https://matrix.foxhound.run
VITE_APP_NAME=Surf Chat
# Calls (MatrixRTC + LiveKit). Enabled by default — backend is live on prod and test.
VITE_CALLS_ENABLED=true
